//
   Created by baizn on 14-10-21.
.wu-example.form-group#uploader
    h4 请上传静态文件
    .col-sm-5
        input(type='file',name='uploadFileName',multiple='multiple').form-control#uploadFileName
    button.btn.btn-default#uploadFile 上传

.col-md-7
    table.table.table-hover
        thead
            tr
                th 序号
                th 文件名
                th 操作
        tbody
            if(result)
                -var i = 1;
                each file,j in result
                    tr
                        td= i++
                        td= file
                        td
                            a(filePath= j).btn-link.btn.zipJsFile 压缩
            else
                tr
                    td(colspan='3',style='text-align:center;color:red;') 没有文件
.col-md-5
    textarea(rows='20').form-control#finalJsCode

script(type='text/javascript').
    seajs.use(['ZipJsFile'],function(ZipJsFile){
        var zipJsFile = new ZipJsFile();
        zipJsFile.init();
    });